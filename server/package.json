{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:seed": "npx wrangler d1 execute translatable-development --local --file=./schema.sql",
    "db:get": "npx wrangler d1 execute translatable-development --local --command=\"SELECT * FROM translations\"",
    "start": "npx wrangler dev ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.6.1"
  },
  "type": "module"
}
